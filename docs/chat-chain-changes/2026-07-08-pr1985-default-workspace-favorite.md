---
date: 2026-07-08
pr: 1985
feature: Default workspace favorite control
impact: New chat workspace selection keeps the default-workspace favorite control inside the selected folder row and completes locale coverage without changing chat session execution or persistence behavior.
---

The folder picker now owns the optional default-workspace favorite affordance so the selected path and favorite state stay visually grouped. Existing default workspace storage, recent workspace selection, and chat launch behavior are unchanged.
